name: Build Docker Images

on:
  schedule:
    - cron: "0 0 * * *"
  pull_request:
    paths-ignore:
      - "*.md"
      - "binder/**"
      - "docs/**"
      - "examples/**"
  push:
    branches:
      - main
    paths-ignore:
      - "*.md"
      - "binder/**"
      - "docs/**"
      - "examples/**"

jobs:
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    if: >
      !contains(github.event.head_commit.message, 'ci skip') &&
      !contains(github.event.pull_request.title, 'ci skip')
    steps:
      - name: Clone Main Repo
        uses: actions/checkout@v2
        # with:
        #   path: main
      - name: Build Docker Images
        run: |
          env
          ls -la
          export REGISTRY_TARGET=${{ secrets.AKT_ENG_REGISTRY_DOMAIN }} && ./build.sh --lint=true

      - name: Login to target registry
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v1
        with:
          registry: https://eu.gcr.io
          username: _json_key
          password: ${{ secrets.AKT_ENG_REGISTRY_PUBLIC_JSON }}

      - name: Push images to GoogleContainerRegistry
        if: github.ref == 'refs/heads/main'
        run: docker images --format "{{.Repository}}:{{.Tag}}" | grep ${{ secrets.AKT_ENG_REGISTRY_DOMAIN }} | xargs -n 1 docker push
